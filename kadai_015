class Human:
    def __init__(self, name, age):
        self.name = name
        self.age = age
# メソッドを定義する
    def printinfo(self):
        print (self.name,self.age)

# インスタンス化する
person = Human("侍太郎", 30)

# 属性にアクセスし、値を出力する
person.printinfo()
